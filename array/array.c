#include <stdio.h>
int main() {
    /**
        const int n  = 10;
        int values[n];

        for (int i = 0; i < n; i++) {
            values[i] = i + 1;
        }
        
        values[5] = 154;
        
        for (int i = 0; i < n; i++)
        {
            printf("%d\n", values[i]);
        }
    **/

    int values[10];
    return 0;
}